<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.mybatis_plus.dao.UserAddressMapper">

    <!-- 开启二级缓存 -->
<!--    <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>-->

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.springboot.mybatis_plus.domain.UserAddress">
        <id column="user_address_id" property="userAddressId" />
        <result column="country" property="country" />
        <result column="province" property="province" />
        <result column="city" property="city" />
        <result column="town" property="town" />
        <result column="address_code" property="addressCode" />
        <result column="post_code" property="postCode" />
        <result column="lat" property="lat" />
        <result column="lng" property="lng" />
        <result column="place" property="place" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        user_address_id, country, province, city, town, address_code, post_code, lat, lng, place
    </sql>
<select id="">
    select
    <include refid="Base_Column_List"></include>
    from
    user_address
</select>
</mapper>
